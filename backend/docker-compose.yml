services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: aaitek
    volumes:
      - aaitek_pgdata:/var/lib/postgresql/data

volumes:
  aaitek_pgdata:
